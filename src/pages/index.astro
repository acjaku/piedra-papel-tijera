---
import Layout from "@/layouts/base.astro";
import Button from "@/components/button.astro";
---

<Layout>
  <main class="flex flex-col gap-6">
    <section class="text-4xl text-center">
      <div id="ganaste" class="hidden">Ganaste 🎉</div>
      <div id="perdiste" class="hidden">Perdiste 😭</div>
      <div id="empate" class="hidden">Empate 😲</div>
    </section>

    <div>Computadora</div>
    <section class="flex gap-6">
      <Button id="cpu-piedra">🪨</Button>

      <Button id="cpu-papel">🧻</Button>

      <Button id="cpu-tijera">✂️</Button>
    </section>

    <div class="text-6xl text-center">🆚</div>

    <div>Jugador</div>
    <section class="flex gap-6">
      <Button id="piedra">🪨</Button>

      <Button id="papel">🧻</Button>

      <Button id="tijera">✂️</Button>
    </section>
  </main>
</Layout>

<script>
  import "@/script/game";
</script>
